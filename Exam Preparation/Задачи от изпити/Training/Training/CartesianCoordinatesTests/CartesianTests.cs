using System;

class CartesianTests
{
    static void Main()
    {
        /* Triangle */
        //double ApointX = double.Parse(Console.ReadLine());
        //double ApointY = double.Parse(Console.ReadLine());
        //double BpointX = double.Parse(Console.ReadLine());
        //double BpointY = double.Parse(Console.ReadLine());
        //double CpointX = double.Parse(Console.ReadLine());
        //double CpointY = double.Parse(Console.ReadLine());

        //double sideAB = Math.Sqrt((Math.Pow((BpointX - ApointX), 2)) + (Math.Pow((BpointY - ApointY), 2)));
        //double sideBC = Math.Sqrt((Math.Pow((CpointX - BpointX), 2)) + (Math.Pow((CpointY - BpointY), 2)));
        //double sideAC = Math.Sqrt((Math.Pow((CpointX - ApointX), 2)) + (Math.Pow((CpointY - ApointY), 2)));

        //double perimeter = (sideAB + sideBC + sideAC) / 2D;
        //double area = Math.Round(Math.Sqrt(perimeter * (perimeter - sideAB) * (perimeter - sideBC) * (perimeter - sideAC)), 1);

        //bool isValidTriangle = ((sideAB + sideBC) > sideAC) && ((sideAB + sideAC) > sideBC) && ((sideBC + sideAC) > sideAB);

        //if (isValidTriangle == true)
        //{
        //    Console.WriteLine("Yes");
        //    Console.WriteLine("{0:F2}", area);
        //}
        //else
        //{
        //    Console.WriteLine("No");
        //    Console.WriteLine("{0:F2}", sideAB);
        //}
    }
}